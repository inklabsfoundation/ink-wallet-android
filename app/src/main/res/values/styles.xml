<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="BtnStyle" parent="Base.Widget.AppCompat.Button">
        <item name="android:minWidth">1dp</item>
        <item name="android:minHeight">1dp</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingBottom">8dp</item>
        <item name="android:textSize">13sp</item>

    </style>

    <style name="BtnStyle.Blue">
        <item name="android:background">@drawable/bg_blue_button</item>
        <item name="android:textColor">@color/btnWhiteTextColor</item>
    </style>

    <style name="BtnStyle.BlueGray">
        <!--<item name="android:background">@drawable/bg_blue_button</item>-->
        <item name="android:background">#595f7f</item>
        <item name="android:textColor">@color/btnWhiteTextColor</item>
    </style>

</resources>
